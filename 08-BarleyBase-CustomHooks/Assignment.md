# Coding Assignment:

- Divide the `Breweries.jsx` into multiple modular code snippets using React Custom Hooks

  - `useSearchBreweries()` - for taking user input and filtering breweries
  - `useFetchBreweries()` - for getting Breweries list via an API call

- Create a dummy `useUserAuth()` hook that handles user Authentication (Login/Logout)

- Perform Bundler level Optimizations such as **Code Splitting** using `lazy()` and `Suspense` features from the `react`
